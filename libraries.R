#libraries are group of functions which are available for performing some specific area tasks
library(dplyr)    
require(dplyr)                    # library or require both can use
library(randomForest)
install.packages("randomFotest")
#remove.packages("agasfahd")      # to uninstall a package

# Inbuilt data sets
data("iris")
head(iris)
tail(iris)
data("airmiles")
head(airmiles)
tail(airmiles)
AirPassengers
data("Titanic")
Titanic

library(MASS)      # Import MASS library

data("Boston")     # After importing MASS library Boston data set can import
head(Boston)
summary(Boston)